<% @messages.each do | message | %>
  <div class="message">
    <h3><small>(by: <%= message.author %>)</small></h3>
    <p>
      <a href="/messages/<%= message.id %>"><%= message.content %></a>
    </p>
  </div>
<% end %>

<a href="/messages/new">Post a new message... It's FREE (for now)!</a>
